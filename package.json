{
    "name": "projet",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "concurrently  \"    cd back && nodemon server     \"      \"  cd client && npm start     \"   "
    },
    "author": "karim",
    "license": "ISC",
    "dependencies": {
        "bootstrap": "^4.6.0",
        "concurrently": "^6.1.0",
        "react-bootstrap": "^1.6.0"
    },
    "devDependencies": {
        "http-proxy-middleware": "^2.0.0"
    }
}
